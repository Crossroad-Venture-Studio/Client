// Imports.
import Column from '../atoms/Column';

export const  ProfileCard = props => {
  const {
    ...other
  } = {};

  // Render.
  return <Column {...other}>
  </Column>;
}

// Default export.
export default ProfileCard;